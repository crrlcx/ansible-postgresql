---
- name: Configure global settings | Combine
  set_fact:
    postgresql_global_config: "{{ postgresql_global_config_defaults | combine(postgresql_global_config_override) | dict2items(key_name='option', value_name='value') }}"
  tags:
    - always

# - name: Configure global settings | set postgresql.conf
  # template:
    # src: postgresql.conf.j2
    # dest: "{{ postgresql_config_path }}/postgresql.conf"
    # owner: "{{ postgresql_user }}"
    # group: "{{ postgresql_group }}"
    # mode: 0600
  # notify:
    # - restart postgresql

- name: Configure global settings.
  lineinfile:
    dest: "{{ postgresql_config_path }}/postgresql.conf"
    regexp: "^#?{{ item.option }} = .+$"
    line: "{{ item.option }} = '{{ item.value }}'"
    state: "{{ item.state | default('present') }}"
  with_items:
    - "{{ postgresql_global_config }}"
  notify:
    - restart postgresql

- name: Configure host based authentication
  template:
    src: "pg_hba.conf.j2"
    dest: "{{ postgresql_config_path }}/pg_hba.conf"
    owner: "{{ postgresql_user }}"
    group: "{{ postgresql_group }}"
    mode: 0600
  notify:
    - restart postgresql

- name: Ensure PostgreSQL unix socket dirs exist
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ postgresql_user }}"
    group: "{{ postgresql_group }}"
    mode: "{{ postgresql_unix_socket_directories_mode }}"
  with_items:
    - "{{ postgresql_unix_socket_directories }}"
